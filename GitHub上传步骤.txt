🚀 如何将项目上传到GitHub
========================================

本地Git已配置完成！✅
提交已完成！✅

现在需要3个步骤将代码推送到GitHub：

========================================
第1步：在GitHub上创建仓库
========================================

1. 打开浏览器，访问：https://github.com

2. 登录你的GitHub账号
   （如果没有账号，先注册一个）

3. 点击右上角的 "+" → "New repository"

4. 填写仓库信息：
   
   Repository name（仓库名）:
   ┌─────────────────────────────┐
   │ haixing-kuang              │  ⚠️ 注意：使用连字符，不能有空格
   └─────────────────────────────┘
   
   Description（描述，可选）:
   ┌─────────────────────────────┐
   │ 🍅 番茄王国 - 学生观察记录平台  │
   └─────────────────────────────┘
   
   选择：
   ○ Public（公开） - 任何人都能看到
   ● Private（私有） - 只有你能看到
   
   ⚠️ 重要：不要勾选以下选项
   ☐ Add a README file
   ☐ Add .gitignore
   ☐ Choose a license
   （我们已经有这些文件了）

5. 点击 "Create repository" 按钮


========================================
第2步：复制仓库地址
========================================

创建完成后，GitHub会显示一个页面，复制仓库地址：

看到类似这样的地址：
https://github.com/你的用户名/haixing-kuang.git

或者使用SSH地址：
git@github.com:你的用户名/haixing-kuang.git


========================================
第3步：推送代码到GitHub
========================================

打开命令行（PowerShell），执行以下命令：

1. 添加远程仓库（把下面的地址替换成你的）
   git remote add origin https://github.com/你的用户名/haixing-kuang.git

2. 推送代码
   git push -u origin master

3. 输入GitHub用户名和密码
   （如果配置了SSH，则不需要输入）


完整命令示例（复制后修改用户名）：
-----------------------------------------
cd "C:\Users\MECHREVO\Desktop\web homework"
git remote add origin https://github.com/你的用户名/haixing-kuang.git
git push -u origin master


========================================
⚠️ 如果遇到问题
========================================

问题1：git push时提示需要登录
解决：
  方法A：使用Personal Access Token（推荐）
    1. GitHub → Settings → Developer settings
    2. Personal access tokens → Tokens (classic)
    3. Generate new token
    4. 选择 repo 权限
    5. 复制生成的token
    6. 推送时用token代替密码

  方法B：配置SSH密钥
    1. 生成密钥：ssh-keygen -t ed25519 -C "你的邮箱"
    2. GitHub → Settings → SSH and GPG keys
    3. 添加公钥（~/.ssh/id_ed25519.pub）
    4. 使用SSH地址推送

问题2：提示 "fatal: remote origin already exists"
解决：
  git remote remove origin
  然后重新添加


问题3：提示 branch name 不匹配
解决：
  git branch -M main
  git push -u origin main


========================================
✅ 推送成功后
========================================

1. 访问你的GitHub仓库页面
   https://github.com/你的用户名/haixing-kuang

2. 你会看到：
   - ✅ 所有文件已上传
   - ✅ README.md会自动显示在首页
   - ✅ 可以分享给别人查看

3. 后续更新代码：
   git add .
   git commit -m "更新说明"
   git push


========================================
🎯 快速命令参考
========================================

# 查看状态
git status

# 添加文件
git add .

# 提交
git commit -m "提交说明"

# 推送
git push

# 拉取最新代码
git pull


========================================
📱 GitHub Desktop（可选）
========================================

如果觉得命令行麻烦，可以使用GitHub Desktop：

1. 下载：https://desktop.github.com
2. 安装并登录GitHub账号
3. File → Add local repository
4. 选择你的项目文件夹
5. 点击 "Publish repository"
6. 完成！


========================================

现在可以开始上传了！🚀
如有问题，随时询问。
